# Intentional Homebrew packages (from `brew leaves` on the Mac host).
# Install with: brew bundle --file=Brewfile  (handled by ./install.sh on macOS)
brew "azure-cli"
brew "fd"
brew "gh"
brew "lazygit"
brew "neovim"
tap "oven-sh/bun"
brew "oven-sh/bun/bun"

# Terminal (uncomment if you want it managed by brew; host currently uses a direct install):
# cask "ghostty"
# cask "font-meslo-lg-nerd-font"  # p10k-recommended Nerd Font
